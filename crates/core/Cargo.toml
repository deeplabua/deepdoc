[package]
name = "deepdoc-core"
description = "Extract clean Markdown, JSON or text from documents — the DeepDoc engine."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords = ["markdown", "extract", "document", "pdf", "docx"]
categories = ["text-processing"]

[dependencies]
calamine = { workspace = true }
csv = { workspace = true }
html5ever = { workspace = true }
markup5ever_rcdom = { workspace = true }
pulldown-cmark = { workspace = true }
quick-xml = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
zip = { workspace = true }

[dev-dependencies]
# The office fixtures are built as real archives inside the tests, so no binary
# blobs live in the repository.
zip = { workspace = true }
