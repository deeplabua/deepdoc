# Placeholder — the real release workflow is GENERATED by cargo-dist.
#
# Session 007 (Phase 7) runs `dist init` in this repo, which overwrites this
# file with the full matrix build: GitHub Release, shell installer, Homebrew
# formula (shared tap `deeplabua/homebrew-tap`) and Scoop manifest. Do not hand-
# edit the generated file afterwards — change `[workspace.metadata.dist]` in
# Cargo.toml and re-run `dist generate`.
#
# Until then this workflow only checks that a release build of a tagged commit
# is sound; it publishes nothing.

name: Release (placeholder)

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  release-build:
    name: release build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: rustup toolchain install stable --profile minimal

      - uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --workspace --release

      - name: Note
        run: echo "Publishing is not wired up yet — see session 007 (cargo-dist)."
